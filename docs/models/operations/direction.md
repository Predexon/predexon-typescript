# Direction

`in` — external→deposit. `out` — deposit→external. `internal` — between deposit and venues.

## Example Usage

```typescript
import { Direction } from "@predexon/sdk/models/operations";

let value: Direction = "internal";
```

## Values

```typescript
"in" | "out" | "internal"
```