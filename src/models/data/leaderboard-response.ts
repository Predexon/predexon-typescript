/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdk-validation-error.js";
import {
  CursorPagination,
  CursorPagination$inboundSchema,
} from "./cursor-pagination.js";
import {
  LeaderboardEntry,
  LeaderboardEntry$inboundSchema,
} from "./leaderboard-entry.js";
import {
  LeaderboardSortBy,
  LeaderboardSortBy$inboundSchema,
} from "./leaderboard-sort-by.js";
import { TimeWindow, TimeWindow$inboundSchema } from "./time-window.js";

/**
 * Response for global leaderboard endpoint.
 */
export type LeaderboardResponse = {
  /**
   * Time window for rolling metrics.
   */
  window: TimeWindow;
  /**
   * Sort options for leaderboard.
   */
  sortBy: LeaderboardSortBy;
  /**
   * Leaderboard entries
   */
  entries: Array<LeaderboardEntry>;
  /**
   * Cursor-based pagination for endpoints that don't support offset.
   */
  pagination: CursorPagination;
};

/** @internal */
export const LeaderboardResponse$inboundSchema: z.ZodMiniType<
  LeaderboardResponse,
  unknown
> = z.pipe(
  z.object({
    window: TimeWindow$inboundSchema,
    sort_by: LeaderboardSortBy$inboundSchema,
    entries: z.array(LeaderboardEntry$inboundSchema),
    pagination: CursorPagination$inboundSchema,
  }),
  z.transform((v) => {
    return remap$(v, {
      "sort_by": "sortBy",
    });
  }),
);

export function leaderboardResponseFromJSON(
  jsonString: string,
): SafeParseResult<LeaderboardResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => LeaderboardResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'LeaderboardResponse' from JSON`,
  );
}
