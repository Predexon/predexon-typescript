/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import * as types from "../../types/primitives.js";
import { SDKValidationError } from "../errors/sdk-validation-error.js";
import {
  OrderbookLevel,
  OrderbookLevel$inboundSchema,
} from "./orderbook-level.js";

/**
 * Single orderbook snapshot.
 *
 * @remarks
 *
 * Note: This model uses MILLISECONDS for timestamps (not seconds) to match
 * the source data format from the orderbook indexer.
 */
export type OrderbookSnapshot = {
  /**
   * Ask price levels, ordered by price
   */
  asks: Array<OrderbookLevel>;
  /**
   * Bid price levels, ordered by price
   */
  bids: Array<OrderbookLevel>;
  /**
   * Orderbook state hash
   */
  hash: string;
  /**
   * Asset/token ID
   */
  assetId: string;
  /**
   * Snapshot timestamp (Unix epoch MILLISECONDS)
   */
  timestamp: number;
  /**
   * Tick size for price levels
   */
  tickSize: string;
  /**
   * When snapshot was indexed (Unix epoch MILLISECONDS)
   */
  indexedAt: number;
  /**
   * Market condition ID / address
   */
  market: string;
};

/** @internal */
export const OrderbookSnapshot$inboundSchema: z.ZodMiniType<
  OrderbookSnapshot,
  unknown
> = z.object({
  asks: z.array(OrderbookLevel$inboundSchema),
  bids: z.array(OrderbookLevel$inboundSchema),
  hash: types.string(),
  assetId: types.string(),
  timestamp: types.number(),
  tickSize: types.string(),
  indexedAt: types.number(),
  market: types.string(),
});

export function orderbookSnapshotFromJSON(
  jsonString: string,
): SafeParseResult<OrderbookSnapshot, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => OrderbookSnapshot$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'OrderbookSnapshot' from JSON`,
  );
}
