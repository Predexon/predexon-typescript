/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import * as types from "../../types/primitives.js";
import { SDKValidationError } from "../errors/sdk-validation-error.js";
import {
  CursorPagination,
  CursorPagination$inboundSchema,
} from "./cursor-pagination.js";
import {
  LeaderboardSortBy,
  LeaderboardSortBy$inboundSchema,
} from "./leaderboard-sort-by.js";
import {
  MarketFilterLogic,
  MarketFilterLogic$inboundSchema,
} from "./market-filter-logic.js";
import { TimeWindow, TimeWindow$inboundSchema } from "./time-window.js";
import {
  WalletFilterEntry,
  WalletFilterEntry$inboundSchema,
} from "./wallet-filter-entry.js";

/**
 * Response for wallet filter by markets endpoint.
 */
export type WalletFilterResponse = {
  /**
   * Market condition IDs used in filter
   */
  markets: Array<string>;
  /**
   * Logic for combining market filters.
   */
  marketLogic: MarketFilterLogic;
  /**
   * Time window for rolling metrics.
   */
  window: TimeWindow;
  /**
   * Sort options for leaderboard.
   */
  sortBy: LeaderboardSortBy;
  /**
   * Matching wallets
   */
  entries: Array<WalletFilterEntry>;
  /**
   * Cursor-based pagination for endpoints that don't support offset.
   */
  pagination: CursorPagination;
};

/** @internal */
export const WalletFilterResponse$inboundSchema: z.ZodMiniType<
  WalletFilterResponse,
  unknown
> = z.pipe(
  z.object({
    markets: z.array(types.string()),
    market_logic: MarketFilterLogic$inboundSchema,
    window: TimeWindow$inboundSchema,
    sort_by: LeaderboardSortBy$inboundSchema,
    entries: z.array(WalletFilterEntry$inboundSchema),
    pagination: CursorPagination$inboundSchema,
  }),
  z.transform((v) => {
    return remap$(v, {
      "market_logic": "marketLogic",
      "sort_by": "sortBy",
    });
  }),
);

export function walletFilterResponseFromJSON(
  jsonString: string,
): SafeParseResult<WalletFilterResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => WalletFilterResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'WalletFilterResponse' from JSON`,
  );
}
