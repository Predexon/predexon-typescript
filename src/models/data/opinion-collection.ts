/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import * as types from "../../types/primitives.js";
import { SDKValidationError } from "../errors/sdk-validation-error.js";

/**
 * Recurring market collection metadata.
 */
export type OpinionCollection = {
  title: string;
  symbol: string;
  frequency: string;
};

/** @internal */
export const OpinionCollection$inboundSchema: z.ZodMiniType<
  OpinionCollection,
  unknown
> = z.object({
  title: types.string(),
  symbol: types.string(),
  frequency: types.string(),
});

export function opinionCollectionFromJSON(
  jsonString: string,
): SafeParseResult<OpinionCollection, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => OpinionCollection$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'OpinionCollection' from JSON`,
  );
}
