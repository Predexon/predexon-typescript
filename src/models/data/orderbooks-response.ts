/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdk-validation-error.js";
import {
  CursorPagination,
  CursorPagination$inboundSchema,
} from "./cursor-pagination.js";
import {
  OrderbookSnapshot,
  OrderbookSnapshot$inboundSchema,
} from "./orderbook-snapshot.js";

/**
 * Orderbooks endpoint response.
 */
export type OrderbooksResponse = {
  snapshots: Array<OrderbookSnapshot>;
  /**
   * Cursor-based pagination for endpoints that don't support offset.
   */
  pagination: CursorPagination;
};

/** @internal */
export const OrderbooksResponse$inboundSchema: z.ZodMiniType<
  OrderbooksResponse,
  unknown
> = z.object({
  snapshots: z.array(OrderbookSnapshot$inboundSchema),
  pagination: CursorPagination$inboundSchema,
});

export function orderbooksResponseFromJSON(
  jsonString: string,
): SafeParseResult<OrderbooksResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => OrderbooksResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'OrderbooksResponse' from JSON`,
  );
}
