/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import * as types from "../../types/primitives.js";
import { SDKValidationError } from "../errors/sdk-validation-error.js";
import {
  CursorPagination,
  CursorPagination$inboundSchema,
} from "./cursor-pagination.js";
import { UmaEvent, UmaEvent$inboundSchema } from "./uma-event.js";
import {
  UmaQuestionStatus,
  UmaQuestionStatus$inboundSchema,
} from "./uma-question-status.js";

/**
 * Single-market response: current status + event history.
 */
export type UmaMarketDetailResponse = {
  conditionId: string;
  /**
   * UMA question status (regular non-sports markets).
   */
  question: UmaQuestionStatus;
  events?: Array<UmaEvent> | undefined;
  /**
   * Cursor-based pagination for endpoints that don't support offset.
   */
  eventsPagination: CursorPagination;
};

/** @internal */
export const UmaMarketDetailResponse$inboundSchema: z.ZodMiniType<
  UmaMarketDetailResponse,
  unknown
> = z.pipe(
  z.object({
    condition_id: types.string(),
    question: UmaQuestionStatus$inboundSchema,
    events: types.optional(z.array(UmaEvent$inboundSchema)),
    events_pagination: CursorPagination$inboundSchema,
  }),
  z.transform((v) => {
    return remap$(v, {
      "condition_id": "conditionId",
      "events_pagination": "eventsPagination",
    });
  }),
);

export function umaMarketDetailResponseFromJSON(
  jsonString: string,
): SafeParseResult<UmaMarketDetailResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => UmaMarketDetailResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'UmaMarketDetailResponse' from JSON`,
  );
}
