/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import * as types from "../../types/primitives.js";
import { SDKValidationError } from "../errors/sdk-validation-error.js";

/**
 * A single tradeable outcome for an Opinion market.
 */
export type OpinionOutcome = {
  label: string;
  tokenId: string;
  predexonId: string;
  price?: number | null | undefined;
};

/** @internal */
export const OpinionOutcome$inboundSchema: z.ZodMiniType<
  OpinionOutcome,
  unknown
> = z.pipe(
  z.object({
    label: types.string(),
    token_id: z._default(types.string(), ""),
    predexon_id: z._default(types.string(), ""),
    price: z.optional(z.nullable(types.number())),
  }),
  z.transform((v) => {
    return remap$(v, {
      "token_id": "tokenId",
      "predexon_id": "predexonId",
    });
  }),
);

export function opinionOutcomeFromJSON(
  jsonString: string,
): SafeParseResult<OpinionOutcome, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => OpinionOutcome$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'OpinionOutcome' from JSON`,
  );
}
