/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import * as types from "../../types/primitives.js";
import { SDKValidationError } from "../errors/sdk-validation-error.js";

/**
 * Individual PnL data point.
 */
export type PnLDataPoint = {
  timestamp: number;
  pnlToDate: number;
};

/** @internal */
export const PnLDataPoint$inboundSchema: z.ZodMiniType<PnLDataPoint, unknown> =
  z.pipe(
    z.object({
      timestamp: types.number(),
      pnl_to_date: types.number(),
    }),
    z.transform((v) => {
      return remap$(v, {
        "pnl_to_date": "pnlToDate",
      });
    }),
  );

export function pnLDataPointFromJSON(
  jsonString: string,
): SafeParseResult<PnLDataPoint, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => PnLDataPoint$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'PnLDataPoint' from JSON`,
  );
}
