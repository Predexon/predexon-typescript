/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import * as types from "../../types/primitives.js";
import { SDKValidationError } from "../errors/sdk-validation-error.js";

/**
 * Individual wallet volume chart data point.
 */
export type WalletVolumeChartDataPoint = {
  /**
   * Unix timestamp in seconds (start of period)
   */
  timestamp: number;
  /**
   * Total USD volume in this period
   */
  totalVolume: number;
  /**
   * BUY-side USD volume in this period
   */
  buyVolume: number;
  /**
   * SELL-side USD volume in this period
   */
  sellVolume: number;
  /**
   * Number of trades in this period
   */
  tradesCount: number;
};

/** @internal */
export const WalletVolumeChartDataPoint$inboundSchema: z.ZodMiniType<
  WalletVolumeChartDataPoint,
  unknown
> = z.pipe(
  z.object({
    timestamp: types.number(),
    total_volume: types.number(),
    buy_volume: types.number(),
    sell_volume: types.number(),
    trades_count: types.number(),
  }),
  z.transform((v) => {
    return remap$(v, {
      "total_volume": "totalVolume",
      "buy_volume": "buyVolume",
      "sell_volume": "sellVolume",
      "trades_count": "tradesCount",
    });
  }),
);

export function walletVolumeChartDataPointFromJSON(
  jsonString: string,
): SafeParseResult<WalletVolumeChartDataPoint, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => WalletVolumeChartDataPoint$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'WalletVolumeChartDataPoint' from JSON`,
  );
}
