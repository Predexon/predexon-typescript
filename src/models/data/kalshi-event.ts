/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import * as types from "../../types/primitives.js";
import { SDKValidationError } from "../errors/sdk-validation-error.js";

/**
 * Event info nested in Kalshi market.
 */
export type KalshiEvent = {
  eventTicker: string;
  seriesTicker?: string | null | undefined;
  title: string;
  subtitle: string;
};

/** @internal */
export const KalshiEvent$inboundSchema: z.ZodMiniType<KalshiEvent, unknown> = z
  .pipe(
    z.object({
      event_ticker: types.string(),
      series_ticker: z.optional(z.nullable(types.string())),
      title: z._default(types.string(), ""),
      subtitle: z._default(types.string(), ""),
    }),
    z.transform((v) => {
      return remap$(v, {
        "event_ticker": "eventTicker",
        "series_ticker": "seriesTicker",
      });
    }),
  );

export function kalshiEventFromJSON(
  jsonString: string,
): SafeParseResult<KalshiEvent, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => KalshiEvent$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'KalshiEvent' from JSON`,
  );
}
