/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import * as types from "../../types/primitives.js";
import { SDKValidationError } from "../errors/sdk-validation-error.js";
import {
  WindowMetrics,
  WindowMetrics$inboundSchema,
} from "./window-metrics.js";

/**
 * Single entry in wallet filter results.
 */
export type WalletFilterEntry = {
  /**
   * Position in results (1-indexed)
   */
  rank: number;
  /**
   * Wallet address
   */
  user: string;
  /**
   * Metrics for a specific time window.
   */
  metrics: WindowMetrics;
  /**
   * Combined price edge across specified markets (avg_sell - avg_buy)
   */
  entryEdge?: number | null | undefined;
  /**
   * Number of specified markets this wallet traded
   */
  marketsMatched: number;
};

/** @internal */
export const WalletFilterEntry$inboundSchema: z.ZodMiniType<
  WalletFilterEntry,
  unknown
> = z.pipe(
  z.object({
    rank: types.number(),
    user: types.string(),
    metrics: WindowMetrics$inboundSchema,
    entry_edge: z.optional(z.nullable(types.number())),
    markets_matched: types.number(),
  }),
  z.transform((v) => {
    return remap$(v, {
      "entry_edge": "entryEdge",
      "markets_matched": "marketsMatched",
    });
  }),
);

export function walletFilterEntryFromJSON(
  jsonString: string,
): SafeParseResult<WalletFilterEntry, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => WalletFilterEntry$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'WalletFilterEntry' from JSON`,
  );
}
