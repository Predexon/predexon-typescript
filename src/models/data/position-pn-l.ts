/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import * as types from "../../types/primitives.js";
import { SDKValidationError } from "../errors/sdk-validation-error.js";

/**
 * Profit and loss for a position.
 */
export type PositionPnL = {
  /**
   * Unrealized P&L in USD
   */
  unrealizedUsd: number;
  /**
   * Unrealized P&L as percentage
   */
  unrealizedPct: number;
  /**
   * Realized P&L in USD
   */
  realizedUsd: number;
};

/** @internal */
export const PositionPnL$inboundSchema: z.ZodMiniType<PositionPnL, unknown> = z
  .pipe(
    z.object({
      unrealized_usd: types.number(),
      unrealized_pct: types.number(),
      realized_usd: types.number(),
    }),
    z.transform((v) => {
      return remap$(v, {
        "unrealized_usd": "unrealizedUsd",
        "unrealized_pct": "unrealizedPct",
        "realized_usd": "realizedUsd",
      });
    }),
  );

export function positionPnLFromJSON(
  jsonString: string,
): SafeParseResult<PositionPnL, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => PositionPnL$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'PositionPnL' from JSON`,
  );
}
