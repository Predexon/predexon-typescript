/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import * as types from "../../types/primitives.js";
import { SDKValidationError } from "../errors/sdk-validation-error.js";
import {
  ClusterSiblingEntry,
  ClusterSiblingEntry$inboundSchema,
} from "./cluster-sibling-entry.js";

/**
 * Connected wallets for a given seed, sourced from the wallet-cluster job.
 */
export type WalletClusterResponse = {
  /**
   * The seed wallet address (lower-cased)
   */
  seed: string;
  /**
   * Top-N siblings by confidence
   */
  siblings: Array<ClusterSiblingEntry>;
  /**
   * Unix timestamp when this cluster was last computed (None if cache miss).
   */
  computedAt?: number | null | undefined;
};

/** @internal */
export const WalletClusterResponse$inboundSchema: z.ZodMiniType<
  WalletClusterResponse,
  unknown
> = z.pipe(
  z.object({
    seed: types.string(),
    siblings: z.array(ClusterSiblingEntry$inboundSchema),
    computed_at: z.optional(z.nullable(types.number())),
  }),
  z.transform((v) => {
    return remap$(v, {
      "computed_at": "computedAt",
    });
  }),
);

export function walletClusterResponseFromJSON(
  jsonString: string,
): SafeParseResult<WalletClusterResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => WalletClusterResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'WalletClusterResponse' from JSON`,
  );
}
