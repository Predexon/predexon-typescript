/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdk-validation-error.js";
import {
  CursorPagination,
  CursorPagination$inboundSchema,
} from "./cursor-pagination.js";
import {
  SmartActivityMarket,
  SmartActivityMarket$inboundSchema,
} from "./smart-activity-market.js";
import { TimeWindow, TimeWindow$inboundSchema } from "./time-window.js";

/**
 * Criteria used to define smart wallets
 */
export type SmartActivityResponseSmartWalletCriteria = {};

/**
 * Response for market discovery endpoint.
 */
export type SmartActivityResponse = {
  /**
   * Criteria used to define smart wallets
   */
  smartWalletCriteria: SmartActivityResponseSmartWalletCriteria;
  /**
   * Time window for rolling metrics.
   */
  window: TimeWindow;
  /**
   * Markets with smart wallet activity
   */
  markets: Array<SmartActivityMarket>;
  /**
   * Cursor-based pagination for endpoints that don't support offset.
   */
  pagination: CursorPagination;
};

/** @internal */
export const SmartActivityResponseSmartWalletCriteria$inboundSchema:
  z.ZodMiniType<SmartActivityResponseSmartWalletCriteria, unknown> = z.object(
    {},
  );

export function smartActivityResponseSmartWalletCriteriaFromJSON(
  jsonString: string,
): SafeParseResult<
  SmartActivityResponseSmartWalletCriteria,
  SDKValidationError
> {
  return safeParse(
    jsonString,
    (x) =>
      SmartActivityResponseSmartWalletCriteria$inboundSchema.parse(
        JSON.parse(x),
      ),
    `Failed to parse 'SmartActivityResponseSmartWalletCriteria' from JSON`,
  );
}

/** @internal */
export const SmartActivityResponse$inboundSchema: z.ZodMiniType<
  SmartActivityResponse,
  unknown
> = z.pipe(
  z.object({
    smart_wallet_criteria: z.lazy(() =>
      SmartActivityResponseSmartWalletCriteria$inboundSchema
    ),
    window: TimeWindow$inboundSchema,
    markets: z.array(SmartActivityMarket$inboundSchema),
    pagination: CursorPagination$inboundSchema,
  }),
  z.transform((v) => {
    return remap$(v, {
      "smart_wallet_criteria": "smartWalletCriteria",
    });
  }),
);

export function smartActivityResponseFromJSON(
  jsonString: string,
): SafeParseResult<SmartActivityResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => SmartActivityResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'SmartActivityResponse' from JSON`,
  );
}
