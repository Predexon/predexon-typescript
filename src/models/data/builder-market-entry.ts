/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import * as types from "../../types/primitives.js";
import { SDKValidationError } from "../errors/sdk-validation-error.js";

/**
 * A market attributed to a builder.
 */
export type BuilderMarketEntry = {
  rank: number;
  conditionId: string;
  volumeUsd: number;
  builderFeeUsd: number;
  tradeCount: number;
  uniqueTraders: number;
};

/** @internal */
export const BuilderMarketEntry$inboundSchema: z.ZodMiniType<
  BuilderMarketEntry,
  unknown
> = z.pipe(
  z.object({
    rank: types.number(),
    condition_id: types.string(),
    volume_usd: types.number(),
    builder_fee_usd: types.number(),
    trade_count: types.number(),
    unique_traders: types.number(),
  }),
  z.transform((v) => {
    return remap$(v, {
      "condition_id": "conditionId",
      "volume_usd": "volumeUsd",
      "builder_fee_usd": "builderFeeUsd",
      "trade_count": "tradeCount",
      "unique_traders": "uniqueTraders",
    });
  }),
);

export function builderMarketEntryFromJSON(
  jsonString: string,
): SafeParseResult<BuilderMarketEntry, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => BuilderMarketEntry$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'BuilderMarketEntry' from JSON`,
  );
}
