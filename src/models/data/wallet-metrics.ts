/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdk-validation-error.js";
import {
  AllTimeMetrics,
  AllTimeMetrics$inboundSchema,
} from "./all-time-metrics.js";
import {
  WindowMetrics,
  WindowMetrics$inboundSchema,
} from "./window-metrics.js";

/**
 * All metrics for a wallet across all time windows.
 */
export type WalletMetrics = {
  /**
   * Metrics for a specific time window.
   */
  oneDay: WindowMetrics;
  /**
   * Metrics for a specific time window.
   */
  sevenDay: WindowMetrics;
  /**
   * Metrics for a specific time window.
   */
  thirtyDay: WindowMetrics;
  /**
   * Extended metrics available only for all-time window.
   */
  allTime: AllTimeMetrics;
};

/** @internal */
export const WalletMetrics$inboundSchema: z.ZodMiniType<
  WalletMetrics,
  unknown
> = z.pipe(
  z.object({
    one_day: WindowMetrics$inboundSchema,
    seven_day: WindowMetrics$inboundSchema,
    thirty_day: WindowMetrics$inboundSchema,
    all_time: AllTimeMetrics$inboundSchema,
  }),
  z.transform((v) => {
    return remap$(v, {
      "one_day": "oneDay",
      "seven_day": "sevenDay",
      "thirty_day": "thirtyDay",
      "all_time": "allTime",
    });
  }),
);

export function walletMetricsFromJSON(
  jsonString: string,
): SafeParseResult<WalletMetrics, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => WalletMetrics$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'WalletMetrics' from JSON`,
  );
}
