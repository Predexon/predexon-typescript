/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import * as types from "../../types/primitives.js";
import { SDKValidationError } from "../errors/sdk-validation-error.js";

/**
 * One bucket in a builder timeseries.
 */
export type BuilderTimeseriesPoint = {
  /**
   * Unix timestamp at bucket start
   */
  timestamp: number;
  volumeUsd: number;
  builderFeeUsd: number;
  tradeCount: number;
};

/** @internal */
export const BuilderTimeseriesPoint$inboundSchema: z.ZodMiniType<
  BuilderTimeseriesPoint,
  unknown
> = z.pipe(
  z.object({
    timestamp: types.number(),
    volume_usd: types.number(),
    builder_fee_usd: types.number(),
    trade_count: types.number(),
  }),
  z.transform((v) => {
    return remap$(v, {
      "volume_usd": "volumeUsd",
      "builder_fee_usd": "builderFeeUsd",
      "trade_count": "tradeCount",
    });
  }),
);

export function builderTimeseriesPointFromJSON(
  jsonString: string,
): SafeParseResult<BuilderTimeseriesPoint, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => BuilderTimeseriesPoint$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'BuilderTimeseriesPoint' from JSON`,
  );
}
