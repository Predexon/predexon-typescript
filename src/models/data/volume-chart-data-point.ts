/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import * as types from "../../types/primitives.js";
import { SDKValidationError } from "../errors/sdk-validation-error.js";

/**
 * Individual volume chart data point.
 */
export type VolumeChartDataPoint = {
  /**
   * Unix timestamp in seconds (start of period)
   */
  timestamp: number;
  /**
   * Total USD volume in this period
   */
  totalVolume: number;
  /**
   * YES-side USD volume in this period
   */
  yesVolume: number;
  /**
   * NO-side USD volume in this period
   */
  noVolume: number;
  /**
   * Number of trades in this period
   */
  tradesCount: number;
};

/** @internal */
export const VolumeChartDataPoint$inboundSchema: z.ZodMiniType<
  VolumeChartDataPoint,
  unknown
> = z.pipe(
  z.object({
    timestamp: types.number(),
    total_volume: types.number(),
    yes_volume: types.number(),
    no_volume: types.number(),
    trades_count: types.number(),
  }),
  z.transform((v) => {
    return remap$(v, {
      "total_volume": "totalVolume",
      "yes_volume": "yesVolume",
      "no_volume": "noVolume",
      "trades_count": "tradesCount",
    });
  }),
);

export function volumeChartDataPointFromJSON(
  jsonString: string,
): SafeParseResult<VolumeChartDataPoint, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => VolumeChartDataPoint$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'VolumeChartDataPoint' from JSON`,
  );
}
