/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import * as types from "../../types/primitives.js";
import { SDKValidationError } from "../errors/sdk-validation-error.js";

/**
 * Single book ticker tick at microsecond granularity.
 *
 * @remarks
 *
 * Sourced from Binance BookTicker stream — best bid/ask with quantities
 * at the moment of each update. Typically 10-26 updates per second per symbol.
 */
export type BinanceTickData = {
  /**
   * ISO 8601 UTC timestamp with microsecond precision (e.g. 2026-01-31T12:00:00.123456Z)
   */
  timestamp: string;
  /**
   * Best bid price in USDT
   */
  bidPrice: number;
  /**
   * Best ask price in USDT
   */
  askPrice: number;
  /**
   * Mid price: (bid + ask) / 2
   */
  midPrice: number;
  /**
   * Bid-ask spread: ask - bid
   */
  spread: number;
  /**
   * Quantity available at best bid
   */
  bidQty: number;
  /**
   * Quantity available at best ask
   */
  askQty: number;
};

/** @internal */
export const BinanceTickData$inboundSchema: z.ZodMiniType<
  BinanceTickData,
  unknown
> = z.pipe(
  z.object({
    timestamp: types.string(),
    bid_price: types.number(),
    ask_price: types.number(),
    mid_price: types.number(),
    spread: types.number(),
    bid_qty: types.number(),
    ask_qty: types.number(),
  }),
  z.transform((v) => {
    return remap$(v, {
      "bid_price": "bidPrice",
      "ask_price": "askPrice",
      "mid_price": "midPrice",
      "bid_qty": "bidQty",
      "ask_qty": "askQty",
    });
  }),
);

export function binanceTickDataFromJSON(
  jsonString: string,
): SafeParseResult<BinanceTickData, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => BinanceTickData$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'BinanceTickData' from JSON`,
  );
}
