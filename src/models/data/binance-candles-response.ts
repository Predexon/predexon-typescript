/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import * as types from "../../types/primitives.js";
import { SDKValidationError } from "../errors/sdk-validation-error.js";
import {
  BinanceCandleData,
  BinanceCandleData$inboundSchema,
} from "./binance-candle-data.js";

/**
 * OHLCV candlestick response for a Binance trading pair.
 *
 * @remarks
 *
 * Intervals 1s and 1m are served from pre-aggregated materialized views.
 * Larger intervals (5m, 15m, 1h, 4h, 1d) are aggregated on-the-fly from 1m data.
 */
export type BinanceCandlesResponse = {
  /**
   * Trading pair (e.g. BTCUSDT)
   */
  symbol: string;
  /**
   * Candle interval (e.g. 15m, 1h)
   */
  interval: string;
  /**
   * Array of candles, ordered by timestamp ASC
   */
  candles: Array<BinanceCandleData>;
};

/** @internal */
export const BinanceCandlesResponse$inboundSchema: z.ZodMiniType<
  BinanceCandlesResponse,
  unknown
> = z.object({
  symbol: types.string(),
  interval: types.string(),
  candles: z.array(BinanceCandleData$inboundSchema),
});

export function binanceCandlesResponseFromJSON(
  jsonString: string,
): SafeParseResult<BinanceCandlesResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => BinanceCandlesResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'BinanceCandlesResponse' from JSON`,
  );
}
