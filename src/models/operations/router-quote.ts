/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { ClosedEnum } from "../../types/enums.js";

export const RouterQuoteServerList = [
  "https://trade.predexon.com",
] as const;

export const Side = {
  Buy: "buy",
  Sell: "sell",
} as const;
export type Side = ClosedEnum<typeof Side>;

export type RouterQuoteRequest = {
  accountId: string;
  predexonId: string;
  side: Side;
  /**
   * USD notional. Required when `side=buy`.
   */
  amount?: number | undefined;
  /**
   * Share count. Required when `side=sell`.
   */
  size?: number | undefined;
};

/** @internal */
export const Side$outboundSchema: z.ZodMiniEnum<typeof Side> = z.enum(Side);

/** @internal */
export type RouterQuoteRequest$Outbound = {
  accountId: string;
  predexonId: string;
  side: string;
  amount?: number | undefined;
  size?: number | undefined;
};

/** @internal */
export const RouterQuoteRequest$outboundSchema: z.ZodMiniType<
  RouterQuoteRequest$Outbound,
  RouterQuoteRequest
> = z.object({
  accountId: z.string(),
  predexonId: z.string(),
  side: Side$outboundSchema,
  amount: z.optional(z.number()),
  size: z.optional(z.number()),
});

export function routerQuoteRequestToJSON(
  routerQuoteRequest: RouterQuoteRequest,
): string {
  return JSON.stringify(
    RouterQuoteRequest$outboundSchema.parse(routerQuoteRequest),
  );
}
