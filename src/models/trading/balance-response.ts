/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import * as types from "../../types/primitives.js";
import { SDKValidationError } from "../errors/sdk-validation-error.js";
import {
  AggregationMeta,
  AggregationMeta$inboundSchema,
} from "./aggregation-meta.js";
import { Balance, Balance$inboundSchema } from "./balance.js";

export type BalanceResponse = {
  balances?: Array<Balance> | undefined;
  /**
   * Per-plane status metadata on aggregated responses. Surfaces partial failures so clients can detect when a venue timed out or errored while others succeeded.
   */
  meta?: AggregationMeta | undefined;
};

/** @internal */
export const BalanceResponse$inboundSchema: z.ZodMiniType<
  BalanceResponse,
  unknown
> = z.pipe(
  z.object({
    balances: types.optional(z.array(Balance$inboundSchema)),
    _meta: types.optional(AggregationMeta$inboundSchema),
  }),
  z.transform((v) => {
    return remap$(v, {
      "_meta": "meta",
    });
  }),
);

export function balanceResponseFromJSON(
  jsonString: string,
): SafeParseResult<BalanceResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => BalanceResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'BalanceResponse' from JSON`,
  );
}
