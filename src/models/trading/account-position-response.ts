/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { safeParse } from "../../lib/schemas.js";
import * as openEnums from "../../types/enums.js";
import { OpenEnum } from "../../types/enums.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import * as types from "../../types/primitives.js";
import { SDKValidationError } from "../errors/sdk-validation-error.js";
import { MarketBag, MarketBag$inboundSchema } from "./market-bag.js";

/**
 * `active` — still trading. `resolved` — settled, awaiting redemption window. `redeemable` — call `/redeem` to claim winnings.
 */
export const AccountPositionResponseStatus = {
  Active: "active",
  Resolved: "resolved",
  Redeemable: "redeemable",
} as const;
/**
 * `active` — still trading. `resolved` — settled, awaiting redemption window. `redeemable` — call `/redeem` to claim winnings.
 */
export type AccountPositionResponseStatus = OpenEnum<
  typeof AccountPositionResponseStatus
>;

/**
 * `null` while the market is still active; `won` or `lost` once resolved.
 */
export const AccountPositionResponseResult = {
  Won: "won",
  Lost: "lost",
} as const;
/**
 * `null` while the market is still active; `won` or `lost` once resolved.
 */
export type AccountPositionResponseResult = OpenEnum<
  typeof AccountPositionResponseResult
>;

export type AccountPositionResponse = {
  venue?: string | undefined;
  /**
   * Canonical per-outcome identifier — same value as on the order responses for this market/outcome. `null` only when the venue identifier can't be normalized.
   */
  predexonId?: string | null | undefined;
  /**
   * Market question/title
   */
  title?: string | null | undefined;
  outcome?: string | undefined;
  /**
   * Number of shares held
   */
  size?: string | undefined;
  /**
   * `active` — still trading. `resolved` — settled, awaiting redemption window. `redeemable` — call `/redeem` to claim winnings.
   */
  status?: AccountPositionResponseStatus | undefined;
  /**
   * `null` while the market is still active; `won` or `lost` once resolved.
   */
  result?: AccountPositionResponseResult | null | undefined;
  /**
   * Weighted-average entry price per share.
   */
  averagePrice?: string | null | undefined;
  /**
   * Latest market price per share for mark-to-market.
   */
  currentPrice?: string | null | undefined;
  /**
   * Mark-to-market value of the held shares.
   */
  currentValue?: string | null | undefined;
  /**
   * Unrealized profit/loss.
   */
  pnl?: string | null | undefined;
  /**
   * Venue-specific market identification. Populate the fields the target venue requires: Polymarket needs `tokenId`; Predict, Opinion, and Limitless need `tokenId` and one other field (`marketId` for Predict and Opinion; `marketSlug` for Limitless); Hyperliquid needs `assetId`. On responses, the bag contains only the fields the venue surfaces (`tokenId`, `marketId`, `ticker`, `outcome`, `assetId`) — request-only fields like `marketSlug` are not echoed back, and Opinion order-list responses contain `marketId` only (placement responses include both).
   */
  market?: MarketBag | undefined;
};

/** @internal */
export const AccountPositionResponseStatus$inboundSchema: z.ZodMiniType<
  AccountPositionResponseStatus,
  unknown
> = openEnums.inboundSchema(AccountPositionResponseStatus);

/** @internal */
export const AccountPositionResponseResult$inboundSchema: z.ZodMiniType<
  AccountPositionResponseResult,
  unknown
> = openEnums.inboundSchema(AccountPositionResponseResult);

/** @internal */
export const AccountPositionResponse$inboundSchema: z.ZodMiniType<
  AccountPositionResponse,
  unknown
> = z.object({
  venue: types.optional(types.string()),
  predexonId: z.optional(z.nullable(types.string())),
  title: z.optional(z.nullable(types.string())),
  outcome: types.optional(types.string()),
  size: types.optional(types.string()),
  status: types.optional(AccountPositionResponseStatus$inboundSchema),
  result: z.optional(z.nullable(AccountPositionResponseResult$inboundSchema)),
  averagePrice: z.optional(z.nullable(types.string())),
  currentPrice: z.optional(z.nullable(types.string())),
  currentValue: z.optional(z.nullable(types.string())),
  pnl: z.optional(z.nullable(types.string())),
  market: types.optional(MarketBag$inboundSchema),
});

export function accountPositionResponseFromJSON(
  jsonString: string,
): SafeParseResult<AccountPositionResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => AccountPositionResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'AccountPositionResponse' from JSON`,
  );
}
