/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import * as types from "../../types/primitives.js";
import { SDKValidationError } from "../errors/sdk-validation-error.js";
import {
  AggregationCancelMeta,
  AggregationCancelMeta$inboundSchema,
} from "./aggregation-cancel-meta.js";

export type CancelAllResponse = {
  /**
   * Number of orders that were cancelled
   */
  cancelled?: number | undefined;
  /**
   * Per-plane status metadata on aggregated cancel-all responses. Each venue entry also reports how many of its orders were cancelled.
   */
  meta?: AggregationCancelMeta | undefined;
};

/** @internal */
export const CancelAllResponse$inboundSchema: z.ZodMiniType<
  CancelAllResponse,
  unknown
> = z.pipe(
  z.object({
    cancelled: types.optional(types.number()),
    _meta: types.optional(AggregationCancelMeta$inboundSchema),
  }),
  z.transform((v) => {
    return remap$(v, {
      "_meta": "meta",
    });
  }),
);

export function cancelAllResponseFromJSON(
  jsonString: string,
): SafeParseResult<CancelAllResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => CancelAllResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'CancelAllResponse' from JSON`,
  );
}
