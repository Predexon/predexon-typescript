/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { safeParse } from "../../lib/schemas.js";
import * as openEnums from "../../types/enums.js";
import { OpenEnum } from "../../types/enums.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import * as types from "../../types/primitives.js";
import { SDKValidationError } from "../errors/sdk-validation-error.js";

/**
 * `provisioning` on first enable (poll until terminal). `active` on a re-enable of an already-provisioned venue. `failed` if provisioning errored synchronously — read the `message` for the reason and retry.
 */
export const EnableVenueResponseStatus = {
  Provisioning: "provisioning",
  Active: "active",
  Failed: "failed",
} as const;
/**
 * `provisioning` on first enable (poll until terminal). `active` on a re-enable of an already-provisioned venue. `failed` if provisioning errored synchronously — read the `message` for the reason and retry.
 */
export type EnableVenueResponseStatus = OpenEnum<
  typeof EnableVenueResponseStatus
>;

export type EnableVenueResponse = {
  venue?: string | undefined;
  /**
   * `provisioning` on first enable (poll until terminal). `active` on a re-enable of an already-provisioned venue. `failed` if provisioning errored synchronously — read the `message` for the reason and retry.
   */
  status?: EnableVenueResponseStatus | undefined;
  message?: string | undefined;
};

/** @internal */
export const EnableVenueResponseStatus$inboundSchema: z.ZodMiniType<
  EnableVenueResponseStatus,
  unknown
> = openEnums.inboundSchema(EnableVenueResponseStatus);

/** @internal */
export const EnableVenueResponse$inboundSchema: z.ZodMiniType<
  EnableVenueResponse,
  unknown
> = z.object({
  venue: types.optional(types.string()),
  status: types.optional(EnableVenueResponseStatus$inboundSchema),
  message: types.optional(types.string()),
});

export function enableVenueResponseFromJSON(
  jsonString: string,
): SafeParseResult<EnableVenueResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => EnableVenueResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'EnableVenueResponse' from JSON`,
  );
}
