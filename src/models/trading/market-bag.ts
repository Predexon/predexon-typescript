/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import * as types from "../../types/primitives.js";
import { SDKValidationError } from "../errors/sdk-validation-error.js";

/**
 * Venue-specific market identification. Populate the fields the target venue requires: Polymarket needs `tokenId`; Predict, Opinion, and Limitless need `tokenId` and one other field (`marketId` for Predict and Opinion; `marketSlug` for Limitless); Hyperliquid needs `assetId`. On responses, the bag contains only the fields the venue surfaces (`tokenId`, `marketId`, `ticker`, `outcome`, `assetId`) — request-only fields like `marketSlug` are not echoed back, and Opinion order-list responses contain `marketId` only (placement responses include both).
 */
export type MarketBag = {
  /**
   * Outcome token ID
   */
  tokenId?: string | undefined;
  /**
   * Numeric market ID
   */
  marketId?: string | undefined;
  /**
   * Market ticker
   */
  ticker?: string | undefined;
  /**
   * Outcome side (e.g., Yes, No)
   */
  outcome?: string | undefined;
  /**
   * Market slug. Request-only — accepted as a hint on order placement, not returned in responses.
   */
  marketSlug?: string | undefined;
  /**
   * Hyperliquid market identifier. Required for hyperliquid orders.
   */
  assetId?: string | undefined;
};

/** @internal */
export const MarketBag$inboundSchema: z.ZodMiniType<MarketBag, unknown> = z
  .object({
    tokenId: types.optional(types.string()),
    marketId: types.optional(types.string()),
    ticker: types.optional(types.string()),
    outcome: types.optional(types.string()),
    marketSlug: types.optional(types.string()),
    assetId: types.optional(types.string()),
  });
/** @internal */
export type MarketBag$Outbound = {
  tokenId?: string | undefined;
  marketId?: string | undefined;
  ticker?: string | undefined;
  outcome?: string | undefined;
  marketSlug?: string | undefined;
  assetId?: string | undefined;
};

/** @internal */
export const MarketBag$outboundSchema: z.ZodMiniType<
  MarketBag$Outbound,
  MarketBag
> = z.object({
  tokenId: z.optional(z.string()),
  marketId: z.optional(z.string()),
  ticker: z.optional(z.string()),
  outcome: z.optional(z.string()),
  marketSlug: z.optional(z.string()),
  assetId: z.optional(z.string()),
});

export function marketBagToJSON(marketBag: MarketBag): string {
  return JSON.stringify(MarketBag$outboundSchema.parse(marketBag));
}
export function marketBagFromJSON(
  jsonString: string,
): SafeParseResult<MarketBag, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => MarketBag$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'MarketBag' from JSON`,
  );
}
