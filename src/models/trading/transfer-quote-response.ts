/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import * as types from "../../types/primitives.js";
import { SDKValidationError } from "../errors/sdk-validation-error.js";

export type TransactionRequest = {
  to?: string | undefined;
  data?: string | undefined;
  value?: string | undefined;
  gasLimit?: string | undefined;
};

/**
 * Populated only for inbound quotes (`from === "external"`). The partner submits `transactionRequest` from their own wallet after granting the `approvalTarget` allowance.
 */
export type Inbound = {
  transactionRequest?: TransactionRequest | undefined;
  /**
   * Address the source token must be approved against before submitting the bridge tx.
   */
  approvalTarget?: string | undefined;
  /**
   * Substitute `{source_tx_hash}` after submitting to track progress.
   */
  explorerLinkTemplate?: string | undefined;
};

/**
 * Quote result. Common fields cover all quote variants; the `inbound` block is populated only when `from === "external"` (so the partner has a signed transaction to submit).
 */
export type TransferQuoteResponse = {
  /**
   * Estimated amount delivered to the destination side.
   */
  expectedAmountReceived?: string | undefined;
  /**
   * Floor on delivered amount given the slippage tolerance.
   */
  minAmountReceived?: string | undefined;
  /**
   * Estimated settlement time.
   */
  estimatedDurationSeconds?: number | undefined;
  /**
   * Effective bridge fee in basis points.
   */
  feeBps?: number | undefined;
  /**
   * Bridge fee in USD.
   */
  feeUsd?: string | undefined;
  /**
   * Underlying bridge provider that quoted this route.
   */
  tool?: string | undefined;
  /**
   * Populated only for inbound quotes (`from === "external"`). The partner submits `transactionRequest` from their own wallet after granting the `approvalTarget` allowance.
   */
  inbound?: Inbound | null | undefined;
};

/** @internal */
export const TransactionRequest$inboundSchema: z.ZodMiniType<
  TransactionRequest,
  unknown
> = z.object({
  to: types.optional(types.string()),
  data: types.optional(types.string()),
  value: types.optional(types.string()),
  gasLimit: types.optional(types.string()),
});

export function transactionRequestFromJSON(
  jsonString: string,
): SafeParseResult<TransactionRequest, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => TransactionRequest$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'TransactionRequest' from JSON`,
  );
}

/** @internal */
export const Inbound$inboundSchema: z.ZodMiniType<Inbound, unknown> = z.object({
  transactionRequest: types.optional(
    z.lazy(() => TransactionRequest$inboundSchema),
  ),
  approvalTarget: types.optional(types.string()),
  explorerLinkTemplate: types.optional(types.string()),
});

export function inboundFromJSON(
  jsonString: string,
): SafeParseResult<Inbound, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => Inbound$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'Inbound' from JSON`,
  );
}

/** @internal */
export const TransferQuoteResponse$inboundSchema: z.ZodMiniType<
  TransferQuoteResponse,
  unknown
> = z.object({
  expectedAmountReceived: types.optional(types.string()),
  minAmountReceived: types.optional(types.string()),
  estimatedDurationSeconds: types.optional(types.number()),
  feeBps: types.optional(types.number()),
  feeUsd: types.optional(types.string()),
  tool: types.optional(types.string()),
  inbound: z.optional(z.nullable(z.lazy(() => Inbound$inboundSchema))),
});

export function transferQuoteResponseFromJSON(
  jsonString: string,
): SafeParseResult<TransferQuoteResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => TransferQuoteResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'TransferQuoteResponse' from JSON`,
  );
}
