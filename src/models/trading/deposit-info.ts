/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { safeParse } from "../../lib/schemas.js";
import { ClosedEnum } from "../../types/enums.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import * as types from "../../types/primitives.js";
import { SDKValidationError } from "../errors/sdk-validation-error.js";

/**
 * Always `base`.
 */
export const DepositInfoChain = {
  Base: "base",
} as const;
/**
 * Always `base`.
 */
export type DepositInfoChain = ClosedEnum<typeof DepositInfoChain>;

/**
 * Always `8453` (Base mainnet).
 */
export const DepositInfoChainId = {
  EightThousandFourHundredAndFiftyThree: 8453,
} as const;
/**
 * Always `8453` (Base mainnet).
 */
export type DepositInfoChainId = ClosedEnum<typeof DepositInfoChainId>;

/**
 * Always `USDC`.
 */
export const DepositInfoToken = {
  Usdc: "USDC",
} as const;
/**
 * Always `USDC`.
 */
export type DepositInfoToken = ClosedEnum<typeof DepositInfoToken>;

export type SupportedChain = {
  name?: string | undefined;
  chainId?: number | undefined;
};

/**
 * Hints for funding the deposit wallet from chains other than Base. Use Quote Transfer with `from: "external"` to get a signed transaction your end user submits.
 */
export type CrossChainDeposit = {
  supportedChains?: Array<SupportedChain> | undefined;
  /**
   * Free-form copy partners can display to depositors.
   */
  instructions?: string | undefined;
};

/**
 * The account's deposit wallet — a single Base USDC address that funds every venue except Hyperliquid.
 */
export type DepositInfo = {
  /**
   * On-chain deposit wallet address. Send USDC on Base here.
   */
  address?: string | undefined;
  /**
   * Always `base`.
   */
  chain?: DepositInfoChain | undefined;
  /**
   * Always `8453` (Base mainnet).
   */
  chainId?: DepositInfoChainId | undefined;
  /**
   * Always `USDC`.
   */
  token?: DepositInfoToken | undefined;
  /**
   * USDC contract address on Base.
   */
  tokenAddress?: string | undefined;
  /**
   * Hints for funding the deposit wallet from chains other than Base. Use Quote Transfer with `from: "external"` to get a signed transaction your end user submits.
   */
  crossChainDeposit?: CrossChainDeposit | undefined;
  /**
   * Current USDC balance at this address on Base, formatted as a decimal string (e.g. "523.40"). Use this to confirm an inbound deposit landed without checking a block explorer.
   */
  balance?: string | undefined;
  /**
   * ISO 8601 timestamp of when `balance` was last read from chain. Use to detect cache-stale reads on the partner side.
   */
  balanceUpdatedAt?: string | undefined;
};

/** @internal */
export const DepositInfoChain$inboundSchema: z.ZodMiniEnum<
  typeof DepositInfoChain
> = z.enum(DepositInfoChain);

/** @internal */
export const DepositInfoChainId$inboundSchema: z.ZodMiniEnum<
  typeof DepositInfoChainId
> = z.enum(DepositInfoChainId);

/** @internal */
export const DepositInfoToken$inboundSchema: z.ZodMiniEnum<
  typeof DepositInfoToken
> = z.enum(DepositInfoToken);

/** @internal */
export const SupportedChain$inboundSchema: z.ZodMiniType<
  SupportedChain,
  unknown
> = z.object({
  name: types.optional(types.string()),
  chainId: types.optional(types.number()),
});

export function supportedChainFromJSON(
  jsonString: string,
): SafeParseResult<SupportedChain, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => SupportedChain$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'SupportedChain' from JSON`,
  );
}

/** @internal */
export const CrossChainDeposit$inboundSchema: z.ZodMiniType<
  CrossChainDeposit,
  unknown
> = z.object({
  supportedChains: types.optional(
    z.array(z.lazy(() => SupportedChain$inboundSchema)),
  ),
  instructions: types.optional(types.string()),
});

export function crossChainDepositFromJSON(
  jsonString: string,
): SafeParseResult<CrossChainDeposit, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => CrossChainDeposit$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'CrossChainDeposit' from JSON`,
  );
}

/** @internal */
export const DepositInfo$inboundSchema: z.ZodMiniType<DepositInfo, unknown> = z
  .object({
    address: types.optional(types.string()),
    chain: types.optional(DepositInfoChain$inboundSchema),
    chainId: types.optional(DepositInfoChainId$inboundSchema),
    token: types.optional(DepositInfoToken$inboundSchema),
    tokenAddress: types.optional(types.string()),
    crossChainDeposit: types.optional(
      z.lazy(() => CrossChainDeposit$inboundSchema),
    ),
    balance: types.optional(types.string()),
    balanceUpdatedAt: types.optional(types.string()),
  });

export function depositInfoFromJSON(
  jsonString: string,
): SafeParseResult<DepositInfo, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => DepositInfo$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'DepositInfo' from JSON`,
  );
}
