/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import * as types from "../../types/primitives.js";
import { SDKValidationError } from "../errors/sdk-validation-error.js";
import { Transfer, Transfer$inboundSchema } from "./transfer.js";

export type TransfersList = {
  transfers?: Array<Transfer> | undefined;
  /**
   * Pagination cursor; pass to the next request as `?cursor=`.
   */
  nextCursor?: string | null | undefined;
};

/** @internal */
export const TransfersList$inboundSchema: z.ZodMiniType<
  TransfersList,
  unknown
> = z.object({
  transfers: types.optional(z.array(Transfer$inboundSchema)),
  nextCursor: z.optional(z.nullable(types.string())),
});

export function transfersListFromJSON(
  jsonString: string,
): SafeParseResult<TransfersList, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => TransfersList$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'TransfersList' from JSON`,
  );
}
