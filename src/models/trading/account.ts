/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { safeParse } from "../../lib/schemas.js";
import { ClosedEnum } from "../../types/enums.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import * as types from "../../types/primitives.js";
import { SDKValidationError } from "../errors/sdk-validation-error.js";
import {
  VenueEntitlement,
  VenueEntitlement$inboundSchema,
} from "./venue-entitlement.js";

export const AccountChain = {
  Base: "base",
} as const;
export type AccountChain = ClosedEnum<typeof AccountChain>;

export const AccountChainId = {
  EightThousandFourHundredAndFiftyThree: 8453,
} as const;
export type AccountChainId = ClosedEnum<typeof AccountChainId>;

export const AccountToken = {
  Usdc: "USDC",
} as const;
export type AccountToken = ClosedEnum<typeof AccountToken>;

/**
 * The account's central deposit wallet — a Base USDC address that funds every venue except Hyperliquid. The address + chain/token info is returned inline so you don't need a second call; for cross-chain deposit guidance (supported source chains, intake addresses), use `GET /deposit-info`. Always present on accounts with a provisioned signing wallet.
 */
export type DepositWallet = {
  /**
   * On-chain wallet address. Send USDC on Base here.
   */
  address?: string | undefined;
  chain?: AccountChain | undefined;
  chainId?: AccountChainId | undefined;
  token?: AccountToken | undefined;
  /**
   * USDC contract address on Base.
   */
  tokenAddress?: string | undefined;
};

export type Account = {
  /**
   * Unique identifier for the account
   */
  accountId?: string | undefined;
  /**
   * ISO 8601 creation timestamp
   */
  createdAt?: string | undefined;
  /**
   * The account's central deposit wallet — a Base USDC address that funds every venue except Hyperliquid. The address + chain/token info is returned inline so you don't need a second call; for cross-chain deposit guidance (supported source chains, intake addresses), use `GET /deposit-info`. Always present on accounts with a provisioned signing wallet.
   */
  depositWallet?: DepositWallet | undefined;
  /**
   * Per-venue entitlements keyed by venue name (e.g., `polymarket`, `predict`, `opinion`, `limitless`, `hyperliquid`). Empty immediately after account creation until `POST /enable` is called.
   */
  venues?: { [k: string]: VenueEntitlement } | undefined;
};

/** @internal */
export const AccountChain$inboundSchema: z.ZodMiniEnum<typeof AccountChain> = z
  .enum(AccountChain);

/** @internal */
export const AccountChainId$inboundSchema: z.ZodMiniEnum<
  typeof AccountChainId
> = z.enum(AccountChainId);

/** @internal */
export const AccountToken$inboundSchema: z.ZodMiniEnum<typeof AccountToken> = z
  .enum(AccountToken);

/** @internal */
export const DepositWallet$inboundSchema: z.ZodMiniType<
  DepositWallet,
  unknown
> = z.object({
  address: types.optional(types.string()),
  chain: types.optional(AccountChain$inboundSchema),
  chainId: types.optional(AccountChainId$inboundSchema),
  token: types.optional(AccountToken$inboundSchema),
  tokenAddress: types.optional(types.string()),
});

export function depositWalletFromJSON(
  jsonString: string,
): SafeParseResult<DepositWallet, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => DepositWallet$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'DepositWallet' from JSON`,
  );
}

/** @internal */
export const Account$inboundSchema: z.ZodMiniType<Account, unknown> = z.object({
  accountId: types.optional(types.string()),
  createdAt: types.optional(types.string()),
  depositWallet: types.optional(z.lazy(() => DepositWallet$inboundSchema)),
  venues: types.optional(z.record(z.string(), VenueEntitlement$inboundSchema)),
});

export function accountFromJSON(
  jsonString: string,
): SafeParseResult<Account, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => Account$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'Account' from JSON`,
  );
}
