/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { PredexonCore } from "../core.js";
import { encodeFormQuery } from "../lib/encodings.js";
import { matchStatusCode } from "../lib/http.js";
import * as M from "../lib/matchers.js";
import { compactMap } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { RequestOptions } from "../lib/sdks.js";
import { extractSecurity, resolveGlobalSecurity } from "../lib/security.js";
import { pathToFunc } from "../lib/url.js";
import * as data from "../models/data/index.js";
import {
  ConnectionError,
  InvalidRequestError,
  RequestAbortedError,
  RequestTimeoutError,
  UnexpectedClientError,
} from "../models/errors/http-client-errors.js";
import { PredexonError } from "../models/errors/predexon-error.js";
import { ResponseValidationError } from "../models/errors/response-validation-error.js";
import { SDKValidationError } from "../models/errors/sdk-validation-error.js";
import * as operations from "../models/operations/index.js";
import { PredictfunListMarketsServerList } from "../models/operations/predictfun-list-markets.js";
import { APICall, APIPromise } from "../types/async.js";
import { Result } from "../types/fp.js";

/**
 * Get Predict Markets
 *
 * @remarks
 * List Predict.fun markets with filtering and sorting.
 *
 * Returns markets from the Predict.fun prediction market platform with support for:
 * - Status filtering (open, closed)
 * - Market variant filtering (DEFAULT, CRYPTO_UP_DOWN, SPORTS_TEAM_MATCH, etc.)
 * - Text search in titles
 * - Bid depth thresholds
 * - Filtering by condition_id, slug, market_id, token_id, event_slug
 * - Multiple sort options
 * - Cursor-based pagination via pagination_key
 *
 * Markets sharing the same slug belong to the same event (e.g. "2026-nba-champion").
 * Use event_slug to query all outcomes under an event.
 *
 * If set, this operation will use {@link Security.dataApiKey} from the global security.
 */
export function predictfunListMarkets(
  client: PredexonCore,
  request?: operations.PredictfunListMarketsRequest | undefined,
  options?: RequestOptions,
): APIPromise<
  Result<
    data.PredictMarketsResponse,
    | data.HTTPValidationError
    | PredexonError
    | ResponseValidationError
    | ConnectionError
    | RequestAbortedError
    | RequestTimeoutError
    | InvalidRequestError
    | UnexpectedClientError
    | SDKValidationError
  >
> {
  return new APIPromise($do(
    client,
    request,
    options,
  ));
}

async function $do(
  client: PredexonCore,
  request?: operations.PredictfunListMarketsRequest | undefined,
  options?: RequestOptions,
): Promise<
  [
    Result<
      data.PredictMarketsResponse,
      | data.HTTPValidationError
      | PredexonError
      | ResponseValidationError
      | ConnectionError
      | RequestAbortedError
      | RequestTimeoutError
      | InvalidRequestError
      | UnexpectedClientError
      | SDKValidationError
    >,
    APICall,
  ]
> {
  const parsed = safeParse(
    request,
    (value) =>
      z.parse(
        z.optional(operations.PredictfunListMarketsRequest$outboundSchema),
        value,
      ),
    "Input validation failed",
  );
  if (!parsed.ok) {
    return [parsed, { status: "invalid" }];
  }
  const payload = parsed.value;
  const body = null;

  const baseURL = options?.serverURL
    || pathToFunc(PredictfunListMarketsServerList[0], {
      charEncoding: "percent",
    })();

  const path = pathToFunc("/v2/predictfun/markets")();

  const query = encodeFormQuery({
    "condition_id": payload?.condition_id,
    "event_slug": payload?.event_slug,
    "limit": payload?.limit,
    "market_id": payload?.market_id,
    "market_slug": payload?.market_slug,
    "market_variant": payload?.market_variant,
    "min_bid_depth": payload?.min_bid_depth,
    "pagination_key": payload?.pagination_key,
    "predexon_id": payload?.predexon_id,
    "search": payload?.search,
    "sort": payload?.sort,
    "status": payload?.status,
    "token_id": payload?.token_id,
  });

  const headers = new Headers(compactMap({
    Accept: "application/json",
  }));

  const securityInput = await extractSecurity(client._options.security);
  const requestSecurity = resolveGlobalSecurity(securityInput, [0]);

  const context = {
    options: client._options,
    baseURL: baseURL ?? "",
    operationID: "predictfunListMarkets",
    oAuth2Scopes: null,

    resolvedSecurity: requestSecurity,

    securitySource: client._options.security,
    retryConfig: options?.retries
      || client._options.retryConfig
      || { strategy: "none" },
    retryCodes: options?.retryCodes || ["429", "500", "502", "503", "504"],
  };

  const requestRes = client._createRequest(context, {
    security: requestSecurity,
    method: "GET",
    baseURL: baseURL,
    path: path,
    headers: headers,
    query: query,
    body: body,
    userAgent: client._options.userAgent,
    timeoutMs: options?.timeoutMs || client._options.timeoutMs || -1,
  }, options);
  if (!requestRes.ok) {
    return [requestRes, { status: "invalid" }];
  }
  const req = requestRes.value;

  const doResult = await client._do(req, {
    context,
    isErrorStatusCode: (statusCode: number) =>
      matchStatusCode({ status: statusCode } as Response, ["4XX", "5XX"]),
    retryConfig: context.retryConfig,
    retryCodes: context.retryCodes,
  });
  if (!doResult.ok) {
    return [doResult, { status: "request-error", request: req }];
  }
  const response = doResult.value;

  const responseFields = {
    HttpMeta: { Response: response, Request: req },
  };

  const [result] = await M.match<
    data.PredictMarketsResponse,
    | data.HTTPValidationError
    | PredexonError
    | ResponseValidationError
    | ConnectionError
    | RequestAbortedError
    | RequestTimeoutError
    | InvalidRequestError
    | UnexpectedClientError
    | SDKValidationError
  >(
    M.json(200, data.PredictMarketsResponse$inboundSchema),
    M.jsonErr(422, data.HTTPValidationError$inboundSchema),
    M.fail("4XX"),
    M.fail("5XX"),
  )(response, req, { extraFields: responseFields });
  if (!result.ok) {
    return [result, { status: "complete", request: req, response }];
  }

  return [result, { status: "complete", request: req, response }];
}
